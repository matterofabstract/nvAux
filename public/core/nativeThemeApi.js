// const { nativeTheme } = require('electron');
// const Store = require('electron-store');

// const store = new Store();

// nativeTheme.on('updated', () => {
//   const { shouldUseDarkColors } = nativeTheme;
//   store.set('shouldUseDarkColors', shouldUseDarkColors);
// });
